Круглая белая кнопка-иконка поверх фото карточки или в тулбарах.

```jsx
<IconButton kind="favorite" active={fav} onClick={toggle} label="В избранное" />
<IconButton kind="compare" active={cmp} label="Сравнить" />
```

Активное сердце — красное (--color-accent), активное сравнение — синее.
